{
  "name": "trello-clone",
  "version": "1.0.0",
  "description": "trello app with web component",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Felixander Loetama",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1",
    "serve": "^11.3.0"
  }
}
